{% extends 'base.html.twig' %}

{% block body %}

    {% if img.filename is not empty %}

        <img src="{{ asset('bundles/platform/img/' ~ img.filename) }}"/>
    {% endif %}
    {{ form_start(form) }}

    {{ form_widget(form) }}
    <input type="submit" value="Edit" />
    {{ form_end(form) }}

{% endblock %}