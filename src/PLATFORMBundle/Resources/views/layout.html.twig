{% extends 'base.html.twig' %}
{% block sidebar %}
    <div class="card-panel sidebar menu-bar green">
        <a href="#" data-activates="slide-out" class="btn btn-floating pulse button-collapse"><i class="material-icons">view_module</i></a>
        <a href="{{ path('chat_profil') }}" class="btn btn-floating right"> <i class="material-icons">chat</i></a>
    </div>
    <ul id="slide-out" class="side-nav grey darken-4">
        <li><div class="row">
                <div class="col s4">
                    <img src="{{ asset('bundles/platform/img/' ~ app.user.file.filename) }}" alt="image"  class="circle responsive-img">   </div>
                <div class="col s7">
                    <span class="white-text name">  {{ app.user.firstName }}</span>
                    <span class="white-text email">{{ app.user.email }}</span>
                </div>

            </div>
        </li>

        <li><a class="btn green" href="{{ path('my_profil') }}">profil</a></li>
        <li><a class="btn green" href="{{ path('platform_homepage') }}">Search</a></li>
        <li><a class="btn green" href="{{ path('chat_profil') }}">Chat</a></li>
        <li><a class="btn green" href="{{ path('connection_profil') }}">Connection<span class="new badge red">2</span></a></li>
        <li><a class="btn waves-effect light-red darken-1" href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
    </ul>

{% endblock %}
{% block footer %}
    <footer class="page-footer green">
        <div class="footer-copyright">
            © 2017 Copyright Text

        </div>
    </footer>
{% endblock %}